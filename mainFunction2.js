function generate(begin,end,array){
    var beginDate = begin; //тут в формате Date
    var endDate = end;//аналогично
    var rate = array.rate;
    var period = array.period;
    switch(priod){
        case "Week": {}
        for (var i = beginDate.getDate(); i< ....;i+7){
            тут функция рандома чисел (i,i+7)
            потом проверка что бы не повторялись числа 
            потом db.collection.insert
            где дата будет newDate.setDate(чгенерированное число)
            и потом 
            
        }
    }
}